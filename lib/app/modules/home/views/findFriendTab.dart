import 'package:flutter/material.dart';

class FindFriendTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('This is FindFriendTab', style: TextStyle(fontSize: 22, color: Colors.green),),
      ),
    );
  }
}